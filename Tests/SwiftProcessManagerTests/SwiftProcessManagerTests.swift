import XCTest
@testable import SwiftProcessManager

final class SwiftProcessManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(SwiftProcessManager().text, "Hello, World!")
    }
}
